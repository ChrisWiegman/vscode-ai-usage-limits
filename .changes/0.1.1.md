## 0.1.1 - 2026-04-11

### Bug Fixes

* Show `Please log in` for installed but unauthenticated providers, replace the generic unavailable state with `No usage yet` when no usage windows have started, and add spacing after 5h:/7d: values in the status bar.

### Chores

* Refresh the README so it matches the current extension behavior, including the visible logged-out state, the `No usage yet` state, percentage-based OAuth modes, and the updated manual testing guidance.
* Update project dependencies.
* Update VSCode engine to 1.115.0
* Add more testing
