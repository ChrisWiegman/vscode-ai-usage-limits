## 0.2.0 - 2026-04-13

### Features

* Add `AI Limits: Refresh Now` command to the Command Palette that clears the shared cache and immediately fetches fresh usage data from the API.

### Chores

* Update project dependencies
